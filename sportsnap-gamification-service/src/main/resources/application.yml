spring:
  application:
    name: sportsnap-gamification-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:sportsnap_gamification}
    username: ${DB_USER:sportsnap}
    password: ${DB_PASS:sportsnap}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8081
