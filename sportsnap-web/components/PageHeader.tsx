type Props = {   title: string;   subtitle?: string;   eyebrow?: string;   children?: React.ReactNode; };  /**  * Cabeçalho de página hero. Raio unificado com o restante do app.  * API (title/subtitle/eyebrow/children) preservada.  */ export function PageHeader({ title, subtitle, eyebrow, children }: Props) {   return (     <section       className="relative mb-10 overflow-hidden rounded-3xl px-6 py-12 fade-up sm:px-12 sm:py-16"       style={{         background: "linear-gradient(145deg, #0a0a0c 0%, #1c1c1e 100%)",         colorScheme: "dark",       }}     >       <div className="absolute -right-24 -top-24 h-80 w-80 rounded-full bg-accent/20 blur-[100px]" />       <div className="absolute -bottom-40 -left-20 h-80 w-80 rounded-full bg-violet-600/10 blur-[100px]" />        <div className="relative flex flex-col justify-between gap-8 md:flex-row md:items-end">         <div className="max-w-2xl">           {eyebrow && (             <p className="mb-4 text-[12px] font-black uppercase tracking-[0.25em] text-accent">               {eyebrow}             </p>           )}           <h1 className="text-4xl font-black leading-[1.1] tracking-tight text-white sm:text-5xl">             {title}           </h1>           {subtitle && (             <p className="mt-5 max-w-xl text-base font-medium leading-relaxed text-ink-300 sm:text-lg">               {subtitle}             </p>           )}         </div>         {children && <div className="flex shrink-0 flex-wrap gap-3">{children}</div>}       </div>     </section>   ); }